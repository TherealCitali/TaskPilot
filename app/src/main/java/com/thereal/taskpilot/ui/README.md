# UI scaffold

The initial Compose shell lives in `TaskPilotApp.kt`. It intentionally uses local mock plans so the navigation and review flow can be explored before the AI, encrypted storage, accessibility tree, and action-validation layers are connected.
